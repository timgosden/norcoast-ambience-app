import SwiftUI

struct ContentView: View {
    var body: some View {
        AmbienceWebView()
            .ignoresSafeArea()
            .background(Color(red: 0.024, green: 0.031, blue: 0.063))
    }
}
